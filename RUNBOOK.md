# payments-api runbook

Demo payments API Helm chart

Generated by repave blueprint `helm-chart-generic`.

## Owner

platform

Primary engineering owner for this chart and its workloads.

## Escalation

1. Page the chart owner via your incident tool.
2. Escalate to platform on-call for cluster or GitOps controller failures.
3. Coordinate with the application team when the workload is **payments-api**.

## Dashboards

- Link dashboards for **payments-api** (CPU, memory, restarts, ingress health).
- Link Argo CD or Flux views when GitOps deploys this chart.

## Rollback procedure

1. Open a GitOps PR reverting `targetRevision` / chart version to the last known good pin.
2. Sync the environment after merge (or wait for automated sync when enabled).
3. Verify pods: `kubectl get pods -l app.kubernetes.io/name=payments-api`.

## Game-day checklist

- [ ] Chart owner and application owner documented
- [ ] GitOps repo and environment protection rules verified
- [ ] `helm template` output reviewed after values changes
- [ ] Rollback via GitOps exercised in the last quarter
