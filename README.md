# payments-api

Demo payments API Helm chart

Generated by repave blueprint `helm-chart-generic`.

## Usage

Install or upgrade the chart:

```bash
helm upgrade --install payments-api . --namespace default
```

Render templates locally:

```bash
helm template payments-api .
```

Lint the chart:

```bash
helm lint .
```

## Configuration

See `values.yaml` for `image`, `service`, and optional `ingress` settings.

## Provenance

- **Blueprint:** `helm-chart-generic` @ `0.3.1`
- **Standard:** `standards/helm/chart-standard.md` @ `1.0.0`
- **Engine:** `2.34.0` (generated `2026-08-07T04:42:22+00:00`)
- **Canonical record:** `repave.yaml` (`provenance-drift` gate validates this file)

